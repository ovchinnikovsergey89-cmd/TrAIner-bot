import os
import platform

def clean_database():
    # Имя файла твоей базы данных (проверь, что в config.py такое же)
    db_file = "database.db"
    
    print(f"🔍 Попытка очистки базы данных: {db_file}")
    
    if os.path.exists(db_file):
        try:
            # Закрываем соединения не нужно, так как мы удаляем файл из ОС
            os.remove(db_file)
            print(f"✅ Файл {db_file} успешно удален.")
            print("🚀 При следующем запуске main.py будет создана чистая база с новой структурой.")
        except Exception as e:
            print(f"❌ Ошибка при удалении файла: {e}")
            print("💡 Убедитесь, что бот остановлен и файл не используется другим процессом.")
    else:
        print(f"⚠️ Файл {db_file} не найден в текущей папке.")
        # Выведем список файлов для диагностики
        print("📁 Содержимое папки:", os.listdir('.'))

if __name__ == "__main__":
    clean_database()