async def search_exercise_video(query_text: str):
    """
    Генерирует ссылку на результаты поиска RuTube.
    Это 'План Б': 100% надежность, так как мы не парсим сайт, 
    а просто отправляем юзера на страницу с результатами.
    """
    # 1. Подготавливаем запрос (меняем пробелы на плюсы)
    # Добавляем "техника выполнения", чтобы отсеять мусор
    clean_query = query_text.strip().replace(" ", "+")
    search_query = f"{clean_query}+техника+выполнения"
    
    # 2. Формируем ссылку
    link = f"https://rutube.ru/search/?query={search_query}"
    
    # 3. Возвращаем данные в том же формате, что ждет бот
    # (Ссылка, Заголовок, Длительность/Описание)
    title = f"🔎 Поиск: {query_text}"
    description = "Много видео 📚" 
    
    return link, title, description