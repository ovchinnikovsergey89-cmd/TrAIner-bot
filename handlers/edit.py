from aiogram import Router

# Этот файл больше не используется, так как вся логика редактирования
# перенесена в handlers/profile.py для удобства пользователя.
# Мы оставляем пустой роутер, чтобы не ломать main.py.

router = Router()